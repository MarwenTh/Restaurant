"use client";
import { UserTable } from "@/components/dashboard/user-table";
import React from "react";

type Props = {};

const page = (props: Props) => {
  return <UserTable />;
};

export default page;
