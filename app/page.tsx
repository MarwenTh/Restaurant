"use client";
import { Navbar } from "@/components/landing-page/navbar";
import React from "react";

type Props = {};

const page = (props: Props) => {
  return (
    <div>
      <Navbar />
    </div>
  );
};

export default page;
