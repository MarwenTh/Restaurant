"use client";
import OrderFailed from "@/components/order/orderFailed";
import React from "react";

type Props = {};

const page = (props: Props) => {
  return <OrderFailed />;
};

export default page;
